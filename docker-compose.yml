version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: G705-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: g705
      POSTGRES_USER: g705user
      POSTGRES_PASSWORD: g705password   
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  backend:
    build:
      context: ./projX
      dockerfile: Dockerfile
    container_name: G705-backend
    restart: unless-stopped
    depends_on:
      - postgres
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/g705
      SPRING_DATASOURCE_USERNAME: g705user
      SPRING_DATASOURCE_PASSWORD: g705password
    init: true
    stop_grace_period: 30s

  frontend:
    build:
      context: ./projX/frontend
      dockerfile: Dockerfile
    container_name: G705-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    stdin_open: true
    tty: true

volumes:
  postgres_data:
