services:
  toname:
    build:
      context: ./projX
      dockerfile: Dockerfile
    container_name: G705
    ports:
      - "8081:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    restart: unless-stopped
    networks:
      - toname-network

networks:
  toname-network:
    driver: bridge
